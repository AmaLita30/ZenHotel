<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head th:replace="fragments/base :: head">
    <meta charset="UTF-8">
    <title>ZenHotel</title>
</head>
<body>
<div th:replace="fragments/base :: base">
    <main class="main" th:fragment="content">
        <!-- DESCRIPCIÓN -->
        <div class=main-description>
            <div>
                <h2>Invoice</h2>
                <p>*List of Invoices.</p>
            </div>
            <a th:href="@{/invoice/create}">
                <span>New Invoice</span>
                <i class="fa-solid fa-plus"></i>
            </a>
        </div>
        <!-- CONTENIDO -->
        <section class="main-content">
            <table>
                <thead>
                <tr>
                    <td>ID</td>
                    <td>Booking Date</td>
                    <td>Payment Method</td>
                    <td>Payment Status</td>
                    <td>Extra Service</td>
                    <td>Amount</td>
                    <td>Payment Date</td>
                    <td>Edit</td>
                </tr>
                </thead>
                <tbody>
                <tr th:each="invoice : ${invoices}">
                    <td th:text="${invoice.idinvoice}"></td>
                    <td th:text="${invoice.booking.bookingdate}"></td>
                    <td th:text="${invoice.paymentmethod.paymentmethodname}"></td>
                    <td th:text="${invoice.paymentstatus.paymentstatusname}"></td>
                    <td th:text="${invoice.extraservice.extraservicename}"></td>
                    <td th:text="${invoice.amount}"></td>
                    <td th:text="${invoice.paymentdate}"></td>
                    <td>
                        <a th:href="@{/invoice/edit/{id}(id=${invoice.idinvoice})} class="edit"> <!--th:href="@{/booking/edit/{id}(id=${booking.idbooking})}"-->
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                    </td>
                </tr>
                </tbody>
            </table>
        </section>
    </main>
</div>
</body>
</html>